---
import { bibliography } from './bibliography';
---

<section aria-labelledby="ohm-references-title">
  <h2 id="ohm-references-title">References</h2>
  <ul>
    {bibliography.map((entry) => <li><a href={entry.url} rel="noreferrer">{entry.name}</a></li>)}
  </ul>
</section>
